<h3>New Weekly Timetable</h3>

<% form_for(@weeklytimetable) do |f| %>
	<%= f.error_messages %>

	<div class="tform">
	<%=render :partial => 'form', :locals => {:f => f} %>
	</div>
	<p>
    	<%= f.submit 'Create' %>
  	</p>

<% end %>

<%= link_to 'Back', weeklytimetables_path %>