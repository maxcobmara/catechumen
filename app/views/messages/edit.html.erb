<h3><%= t 'events.edit' %></h3>

<% form_for(@message) do |f| %>
  <%= f.error_messages %>

  <%=render :partial => 'form', :locals => {:f => f} %> 

  <p>
    <%= f.submit (t 'update') %>
  </p>
<% end %>

<%= link_to (t 'show'), @message %> |
<%= link_to (t 'back'), messages_path %>