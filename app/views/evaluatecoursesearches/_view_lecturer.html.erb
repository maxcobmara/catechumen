<% unless params[:subjectid].blank?%>
  <%= collection_select (:evaluatecoursesearch, :lecturer_id, @lecturer_list, :id, :name, :include_blank=> (t 'select') )%>
<% else %>
  <%= collection_select (:evaluatecoursesearch, :lecturer_id, @lecturer_list2, :id, :name, :include_blank=> (t 'select') )%>
<% end %>
