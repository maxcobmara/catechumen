<h3>Editing Booking Vehicle</h3>

<% form_for(@bookingvehicle) do |f| %>
  <%= f.error_messages %>
<%=render :partial => 'form', :locals => {:f => f} %> 
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @bookingvehicle %> |
<%= link_to 'Back', bookingvehicles_path %>