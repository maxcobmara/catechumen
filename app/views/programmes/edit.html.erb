<h3>Editing Programme</h3>

<% form_for(@programme) do |f| %>
  <%= f.error_messages %>

    <%=render :partial => 'form', :locals => {:f => f} %> 
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>


<%= link_to 'Show', @programme %> |
<%= link_to 'Back', programmes_path %>