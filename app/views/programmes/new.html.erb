<% form_for(@programme) do |f| %>
  <%= f.error_messages %>
	<div class="tform">
		<H2>New Programme Object</H2>
		<%= f.hidden_field :parent_id %>
		<%=render :partial => 'form', :locals => {:f => f} %>
	</div>
  
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', programmes_path %>