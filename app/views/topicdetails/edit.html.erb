<% form_for(@topicdetail, :html => { :multipart => true }) do |f| %>
  <%= f.error_messages %>
	<h3><%=t('topicdetail.edit')%></h3>
	<!--<div class="tform">-->
		<!--<H2>New Topic Detail</H2>-->
		<%=render :partial => 'form', :locals => {:f => f} %>
	<!--</div>-->
  <p>
    <%= f.submit t('update') %>
  </p>
<% end %>

<%= link_to t('show'), @topicdetail %> |
<%= link_to t('back'), topicdetails_path %>