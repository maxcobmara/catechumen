<%#= f.collection_select :subject_id, Subject.find(:all), :id, :name, :include_blank => true %>	
<% unless @programme_id.blank? %>
		<%= collection_select (:klass,:programme_id, @subjects, :id, :name, :prompt => "Select a subject") %>
<% else %>
		&nbsp;&nbsp; Select a programme first.
<% end%>