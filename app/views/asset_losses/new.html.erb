<% form_for(@asset_loss) do |f| %>
  <%= f.error_messages %>
	<div class="tform">
		<H2><%= t 'asset_losses.new' %></H2>
				<%=render :partial => 'form', :locals => {:f => f} %> 
	</div>


  <p>
    <%= f.submit (t 'icms.submit') %>
  </p>
<% end %>

<%= link_to (t 'icms.back'), asset_losses_path %>