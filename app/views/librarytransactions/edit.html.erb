<h3><%= t 'librarytransaction.edit' %></h3>

<% form_for(@librarytransaction) do |f| %>
  <%= f.error_messages %>

  <%=render :partial => 'form', :locals => {:f => f} %> 
  <p>
    <%= f.submit (t 'update') %>
  </p>
<% end %>

<%= link_to (t 'show'), @librarytransaction %> |
<%= link_to (t 'back'), librarytransactions_path %>