<h1>New trainingreport</h1>

<% form_for(@trainingreport) do |f| %>
  <%= f.error_messages %>
  <%= f.hidden_field :timetable_id%>

  <%= render :partial => 'form', :locals => {:f => f} %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', trainingreports_path %>