<% unless @subject_id.blank? %>
		<%= collection_select (:examquestion, :topic_id, @topics.sort_by{|x|x.code}, :id, :subject_list, :include_blank => "Select a topic") %>
<% else %>
		&nbsp;&nbsp; Select a subject first.
<% end%>