<% unless @subject_id.blank? %>
		<%= collection_select (:examquestion, :topic_id, @topics.sort_by{|x|x.code}, :id, :subject_list, :include_blank => (t 'examquestion.select_a_topic1')) %>
<% else %>
		&nbsp;&nbsp; <%=t('examquestion.select_a_subject2')%>
<% end%>