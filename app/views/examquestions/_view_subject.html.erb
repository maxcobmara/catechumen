<% unless @programme_id.blank? %>
		<%= collection_select (:examquestion, :subject_id, @subjects, :id, :subject_code_with_subject_name, :prompt => "Select a subject") %>
<% else %>
		&nbsp;&nbsp; Select a programme first.
<% end%>