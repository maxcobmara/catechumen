<h3>Editing Disposal</h3>

<% form_for(@disposal) do |f| %>
  <%= f.error_messages %>


<%=render :partial => 'form', :locals => {:f => f} %> 
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>
</div>

<%= link_to 'Show', @disposal %> |
<%= link_to 'Back', disposals_path %>