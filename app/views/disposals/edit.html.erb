<h3><%= t 'disposal.edit' %></h3>

<% form_for(@disposal) do |f| %>
  <%= f.error_messages %>


<%=render :partial => 'form', :locals => {:f => f} %> 
  <p>
    <%= f.submit (t 'icms.update') %>
  </p>
<% end %>


<%= link_to (t 'icms.show'), @disposal %> |
<%= link_to (t 'icms.back'), disposals_path %>