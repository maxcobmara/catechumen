<h3><%=t('student_attendance.edit')%></h3>
<% form_for(@student_attendance) do |f| %>
  <%= f.error_messages %>
   <%=render :partial => 'form', :locals => {:f => f} %> 
  <p>
    <%= f.submit (t 'update') %>
  </p>
<% end %>
<%= link_to (t 'back'), student_attendances_path %> | <%= link_to (t 'show'), @student_attendance %>










