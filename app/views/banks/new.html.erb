<% form_for(@bank) do |f| %>
  <%= f.error_messages %>
	
	<div class="tform">
		<H2><%= t 'banks.new_bank' %></H2>
				<%=render :partial => 'form', :locals => {:f => f} %> 
	</div>
  <p>
    <%= f.submit (t 'create') %>
  </p>
<% end %>

<%= link_to (t 'back'), banks_path %>