<h3><%= t 'studentdiscipline.edit' %></h3>

<% form_for(@student_discipline_case) do |f| %>
  <%= f.error_messages %>

 <%=render :partial => 'form', :locals => {:f => f} %> 
  <p>
    <%= f.submit (t 'update') %>
  </p>
<% end %>

<%= link_to (t 'show'), @student_discipline_case %> |
<%= link_to (t 'back'), student_discipline_cases_path %>