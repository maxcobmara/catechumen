<% form_for(@employgrade) do |f| %>
  <%= f.error_messages %>
	<div class="tform">
		<H2>New Staff Employee Group</H2>
				<%=render :partial => 'form', :locals => {:f => f} %> 
	</div>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', employgrades_path %>