<h3>Editing Employ Grade</h3>

<% form_for(@employgrade) do |f| %>
  <%= f.error_messages %>
	<%=render :partial => 'form', :locals => {:f => f} %> <!--27Feb2013-moved to partial-->
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @employgrade %> |
<%= link_to 'Back', employgrades_path %>