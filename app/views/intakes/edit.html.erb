<% form_for(@intake) do |f| %>
	<%= f.error_messages %>
	<div class="tform">
		<H2>Editing intake</H2>
		<%=render :partial => 'form', :locals => {:f => f} %>
	</div>
  	<p>
    	<%= f.submit 'Update' %>
  	</p>
<% end %>

<%= link_to 'Show', @intake %> |
<%= link_to 'Back', intakes_path %>