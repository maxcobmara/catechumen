<h3><%= t 'travel.edit' %></h3>


<% form_for(@travelrequest) do |f| %>
  <%= f.error_messages %>


<%=render :partial => 'form', :locals => {:f => f} %> 
  <p>
    <%= f.submit (t 'icms.update') %>
  </p>
<% end %>

<%= link_to (t 'icms.show'), @travelrequest %> |
<%= link_to (t 'icms.back'), travelrequests_path %>