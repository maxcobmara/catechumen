<% unless params[:lecturerid].blank?%>
  <%= collection_select (:lessonplansearch, :subject, @subject_list, :id, :subject_list, :include_blank => (t 'select'))%>
  <!--<%#@tt.each do |x|%><%#=x%> | <%# end %><%#=@intake2%>==<%#=@ssubjectids%>-->
<% else %>
  <%=t('equery.lesson_plan.select_lecturer_first')%>
<% end %>