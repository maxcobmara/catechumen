<h3><%=t('grade.edit')%></h3>

<% form_for(@grade) do |f| %>
  <%= f.error_messages %>

 <%=render :partial => 'form', :locals => {:f => f} %> 

  <p>
    <%= f.submit (t 'update') %>
  </p>
<% end %>
<%= link_to (t 'show'), @grade %> |
<%= link_to (t 'back'), grades_path %>