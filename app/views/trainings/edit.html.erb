<% form_for(@training) do |f| %>
  <%= f.error_messages %>
	<div class="tform">
		<H2>Edit Training Object</H2>
		<%=render :partial => 'form', :locals => {:f => f} %>
	</div>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @training %> |
<%= link_to 'Back', trainings_path %>