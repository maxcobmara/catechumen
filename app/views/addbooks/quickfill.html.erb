<% form_for :addbook do |f| %>
  <%= f.error_messages %>
	<div class="tform">
		<H2>New Address</H2>
 		<%=render :partial => 'form', :locals => {:f => f} %> 
		<%#= submit_tag "Create", :onclick => "window.close()", :id => "clickety" %>
		<%= f.submit 'Create', :id => "clickety"  %>
	</div>
	<% end %>
	</div>
	
