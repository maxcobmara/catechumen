<h3>New Book</h3>

<% form_for(@book, :html => { :multipart => true}) do |f| %>
<%= f.error_messages %>

  <%=render :partial => 'form', :locals => {:f => f} %> 
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>


<%= link_to 'Back', books_path %>