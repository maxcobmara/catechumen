<h3><%= t 'supplier.new' %></h3>

<% form_for(@stationery) do |f| %>
  <%= f.error_messages %>

   <%=render :partial => 'form', :locals => {:f => f} %> 
  <p>
     <%= f.submit (t 'icms.submit') %>
  </p>
<% end %>

<%= link_to (t 'icms.back'), stationeries_path %>