<% unless params[:programmeid].blank? %>
  <%= select(:examresultsearch, :semester, @semester_list, :include_blank=> (t 'select') )%>
<% else %>
  <%= select(:examresultsearch, :semester, Examresult::SEMESTER, :include_blank=> (t 'select') ) %>
<% end %>