<h3><%= t 'event.new' %></h3>

<% form_for(@event) do |f| %>
  <%= f.error_messages %>

<%=render :partial => 'form', :locals => {:f => f} %> 
  
  <p>
    <%= f.submit (t 'icms.submit') %>
  </p>
<% end %>
</div>

<%= link_to (t 'icms.back'), events_path %>