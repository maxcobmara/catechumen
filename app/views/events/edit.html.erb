<h3><%= t 'event.edit' %></h3>

<% form_for(@event) do |f| %>
  <%= f.error_messages %>

  <%=render :partial => 'form', :locals => {:f => f} %> 
  <p>
    <%= f.submit (t 'icms.update') %>
  </p>
<% end %>
</div>

<%= link_to (t 'icms.show'), @event %> |
<%= link_to (t 'icms.back'), events_path %>