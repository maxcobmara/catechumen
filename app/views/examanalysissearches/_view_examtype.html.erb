<% unless params[:subjectid].blank?%>
  <%= select (:examanalysissearch, :examtype, @examtype_array, :include_blank=> (t 'select') )%>
<% else %>
  <%= select (:examanalysissearch, :examtype, @examtype_array, :include_blank=> (t 'select') )%>
<% end %>
