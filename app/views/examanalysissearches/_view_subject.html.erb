<% unless params[:programmeid].blank?%>
  <%= collection_select (:examanalysissearch, :subject_id, @subject_list, :id, :subject_list, {:include_blank => (t 'select')},
		:onchange => remote_function(:update => form_tag_id(:examanalysissearch, :examtype_list), 
		:url => {:action => 'view_examtype' }, 
		:with => "'subjectid='+$('"+form_tag_id(:examanalysissearch, :subject_id)+"').value")) %>
<% else %>
  <%= collection_select (:examanalysissearch, :subject_id, @subject_list2, :id, :subject_list, {:include_blank => (t 'select')},
		:onchange => remote_function(:update => form_tag_id(:examanalysissearch, :examtype_list), 
		:url => {:action => 'view_examtype' }, 
		:with => "'subjectid='+$('"+form_tag_id(:examanalysissearch, :subject_id)+"').value")) %>
<% end %>