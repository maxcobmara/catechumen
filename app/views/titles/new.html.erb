<% form_for(@title) do |f| %>
  <%= f.error_messages %>
	<div class="tform">
		<H2><%= t 'title.new_title' %></H2>
				<%=render :partial => 'form', :locals => {:f => f} %> 
	</div>
  <p>
    <%= f.submit (t 'create') %>
  </p>
<% end %>

<%= link_to (t 'back'), titles_path %>