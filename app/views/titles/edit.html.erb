<h3><%=t('addressbook.edit')%></h3>
<% form_for(@title) do |f| %>
  <%= f.error_messages %>
	<%=render :partial => 'form', :locals => {:f => f} %> 
  <p>
    <%= f.submit (t 'update') %>
  </p>
<% end %>

<%= link_to (t 'show'), @title %> |
<%= link_to (t 'back'), titles_path %>