<h3><%=t('evaluate_course.new')%></h3>

<% form_for(@evaluate_course) do |f| %>
  <%= f.error_messages %>

<%=render :partial => 'form', :locals => {:f => f} %> 

  <p>
    <%= f.submit (t 'create') %>
  </p>
<% end %>

<%= link_to (t 'back'), evaluate_courses_path %>