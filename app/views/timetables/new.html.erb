<% form_for(@timetable) do |f| %>
  <%= f.error_messages %>
	<div class="tform">
		<H2>New Timetable</H2>
		<%=render :partial => 'form', :locals => {:f => f} %>
	</div>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', timetables_path %>