<h1>New ptschedule</h1>

<% form_for(@ptschedule) do |f| %>
  <%= f.error_messages %>

	<%=render :partial => 'form', :locals => {:f => f} %> 


  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', ptschedules_path %>