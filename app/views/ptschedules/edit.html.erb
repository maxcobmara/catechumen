<h3><%= t 'ptschedule.edit' %></h3>

<% form_for(@ptschedule) do |f| %>
  <%= f.error_messages %>

	<%=render :partial => 'form', :locals => {:f => f} %> 

  <p>
    <%= f.submit (t 'update') %>
  </p>
<% end %>

<%= link_to (t 'show'), @ptschedule %> |
<%= link_to (t 'back'), ptschedules_path %>