<h3>Editing Instructor Appraisal</h3>

<% form_for(@instructor) do |f| %>
  <%= f.error_messages %>

<%=render :partial => 'form', :locals => {:f => f} %> 

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @instructor %> |
<%= link_to 'Back', instructors_path %>