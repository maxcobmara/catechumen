
<% form_for(@position) do |f| %>
  <%= f.error_messages %>
	<div class="tform">
	<H2><%= t 'positions.edit' -%></H2>
  <%=render :partial => 'form', :locals => {:f => f} %> 
	</div>
  <p>
    <%= f.submit (t 'update') %>
  </p>
<% end %>
</div>

<%= link_to (t 'show'), @position %> |
<%= link_to (t 'back'), positions_path %>