<% unless params[:department2].blank? %>
	<%= collection_select(:ptdosearch, :staff_id, @staff_list, :id, :name, :include_blank=> (t 'select') )%>
<% else %>
	<%=text_field :ptdosearch, :staff_name %>
<% end %>