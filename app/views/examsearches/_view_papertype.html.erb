<% unless params[:examtype2].blank?%>
	<%= select (:examsearch, :klass_id, @papertype_array, :include_blank=> (t 'select') )%>
<% else %>
	<%= select (:examsearch, :klass_id, @papertype_array, :include_blank=> (t 'select') )%>
<% end %>