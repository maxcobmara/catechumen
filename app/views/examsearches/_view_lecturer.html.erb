<% unless params[:subjectid].blank?%>
  <%= collection_select (:examsearch, :created_by, @lecturer_list.sort_by{|x|x.name}, :id, :name, :include_blank=> (t 'select') )%>
<% else %>
  <%= collection_select (:examsearch, :created_by, @lecturer_list2.sort_by{|x|x.name}, :id, :name, :include_blank=> (t 'select') )%>
<% end %>
