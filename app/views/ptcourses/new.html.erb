<h3><%= t 'ptcourse.new' %></h3>

<% form_for(@ptcourse) do |f| %>
  <%= f.error_messages %>

	<%=render :partial => 'form', :locals => {:f => f} %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', ptcourses_path %>