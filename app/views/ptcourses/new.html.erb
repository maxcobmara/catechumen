<h3><%= t 'ptcourse.new' %></h3>

<% form_for(@ptcourse) do |f| %>
  <%= f.error_messages %>

	<%=render :partial => 'form', :locals => {:f => f} %> 
  <p>
    <%= f.submit (t 'icms.submit') %>
  </p>
<% end %>

<%= link_to (t 'icms.back'), ptcourses_path %>