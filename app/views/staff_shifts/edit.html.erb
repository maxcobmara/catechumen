<% form_for(@staff_shift) do |f| %>
  <%= f.error_messages %>
	<div class="tform">
		<H2><%= t 'staffshift.edit' %></H2> 
		<%=render :partial => 'form', :locals => {:f => f} %> 
	</div>
  <p>
    <%= f.submit (t 'update') %>
  </p>
<% end %>

<%= link_to (t 'show'), @staff_shift %> |
<%= link_to (t 'back'), staff_shifts_path %>