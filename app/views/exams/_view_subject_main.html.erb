<% unless @programme_id.blank? %>
		<%= collection_select (:exam, :subject_id, @subjects, :id, :subject_list, :include_blank => (t 'select')) %>
<% else %>		
	&nbsp;&nbsp; <%=t('exam.select_programme_first')%>
<% end%>